<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>🌸 樱色幻想 - 动漫主题站</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=M+PLUS+Rounded+1c:wght@300;400;500;700&family=Ma+Shan+Zheng&display=swap" rel="stylesheet">
    <style>
        :root {
            --sakura-pink: #ffb7c5;
            --sakura-light: #ffe6ee;
            --sakura-dark: #ff85a2;
            --anime-blue: #a0d8ef;
            --anime-purple: #c8a2c8;
            --text-dark: #5a4a4a;
        }
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'M PLUS Rounded 1c', sans-serif;
            background: linear-gradient(135deg, #fff0f5 0%, #ffe6ee 50%, #f0f8ff 100%);
            color: var(--text-dark);
            min-height: 100vh;
            overflow-x: hidden;
            position: relative;
        }
        
        /* 樱花飘落效果 */
        .sakura {
            position: fixed;
            top: -50px;
            z-index: 9999;
            pointer-events: none;
            animation: fall linear infinite;
        }
        
        @keyframes fall {
            to {
                transform: translateY(100vh) rotate(360deg);
            }
        }
        
        /* 导航栏 */
        .anime-nav {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            border-bottom: 3px solid var(--sakura-pink);
            box-shadow: 0 4px 20px rgba(255, 183, 197, 0.2);
            position: sticky;
            top: 0;
            z-index: 1000;
        }
        
        .nav-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 1rem 2rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        
        .logo {
            font-family: 'Ma Shan Zheng', cursive;
            font-size: 2rem;
            background: linear-gradient(45deg, var(--sakura-pink), var(--anime-purple));
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.1);
        }
        
        .logo i {
            margin-right: 10px;
        }
        
        .nav-links {
            display: flex;
            gap: 2rem;
        }
        
        .nav-links a {
            text-decoration: none;
            color: var(--text-dark);
            font-weight: 500;
            padding: 0.5rem 1rem;
            border-radius: 50px;
            transition: all 0.3s ease;
            position: relative;
        }
        
        .nav-links a:hover {
            background: var(--sakura-pink);
            color: white;
            transform: translateY(-3px);
        }
        
        .nav-links a::after {
            content: '';
            position: absolute;
            bottom: -5px;
            left: 50%;
            transform: translateX(-50%);
            width: 0;
            height: 3px;
            background: var(--sakura-pink);
            transition: width 0.3s ease;
        }
        
        .nav-links a:hover::after {
            width: 70%;
        }
        
        /* 英雄区域 */
        .hero {
            padding: 6rem 2rem;
            text-align: center;
            position: relative;
            overflow: hidden;
        }
        
        .hero-bg {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: url('https://images.unsplash.com/photo-1545569341-9eb8b30979d9?auto=format&fit=crop&w=1350&q=80') center/cover;
            opacity: 0.1;
            z-index: -1;
        }
        
        .hero-title {
            font-size: 3.5rem;
            margin-bottom: 1rem;
            background: linear-gradient(45deg, var(--sakura-pink), var(--anime-blue));
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            text-shadow: 3px 3px 6px rgba(0,0,0,0.1);
        }
        
        .hero-subtitle {
            font-size: 1.5rem;
            margin-bottom: 2rem;
            color: var(--anime-purple);
        }
        
        /* 动漫卡片 */
        .anime-grid {
            max-width: 1200px;
            margin: 0 auto;
            padding: 2rem;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 2rem;
        }
        
        .anime-card {
            background: white;
            border-radius: 20px;
            overflow: hidden;
            box-shadow: 0 10px 30px rgba(255, 183, 197, 0.2);
            transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            border: 2px solid transparent;
            position: relative;
        }
        
        .anime-card:hover {
            transform: translateY(-10px) scale(1.02);
            border-color: var(--sakura-pink);
            box-shadow: 0 20px 40px rgba(255, 183, 197, 0.4);
        }
        
        .card-header {
            height: 180px;
            background: linear-gradient(135deg, var(--sakura-pink), var(--anime-blue));
            position: relative;
            overflow: hidden;
        }
        
        .card-header::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255,255,255,0.3), transparent);
            transition: 0.5s;
        }
        
        .anime-card:hover .card-header::before {
            left: 100%;
        }
        
        .card-icon {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            font-size: 4rem;
            color: white;
            opacity: 0.8;
        }
        
        .card-body {
            padding: 1.5rem;
        }
        
        .card-title {
            font-size: 1.5rem;
            margin-bottom: 0.5rem;
            color: var(--sakura-dark);
        }
        
        .card-text {
            color: #666;
            line-height: 1.6;
        }
        
        /* 特殊按钮 */
        .anime-btn {
            display: inline-block;
            padding: 12px 30px;
            background: linear-gradient(45deg, var(--sakura-pink), var(--anime-purple));
            color: white;
            border: none;
            border-radius: 50px;
            font-size: 1.1rem;
            font-weight: bold;
            cursor: pointer;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
            box-shadow: 0 5px 15px rgba(255, 183, 197, 0.4);
        }
        
        .anime-btn:hover {
            transform: translateY(-3px) scale(1.05);
            box-shadow: 0 10px 25px rgba(255, 183, 197, 0.6);
        }
        
        .anime-btn:active {
            transform: translateY(1px);
        }
        
        .anime-btn::after {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            width: 0;
            height: 0;
            background: rgba(255, 255, 255, 0.3);
            border-radius: 50%;
            transform: translate(-50%, -50%);
            transition: width 0.3s, height 0.3s;
        }
        
        .anime-btn:hover::after {
            width: 200px;
            height: 200px;
        }
        
        /* 页脚 */
        .anime-footer {
            background: linear-gradient(135deg, var(--sakura-light), var(--anime-blue));
            padding: 3rem 2rem;
            margin-top: 4rem;
            text-align: center;
            position: relative;
            overflow: hidden;
        }
        
        .footer-bg {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100" preserveAspectRatio="none"><path d="M0,0 L100,0 L100,100 Z" fill="rgba(255,255,255,0.1)"/></svg>');
            background-size: cover;
        }
        
        .footer-content {
            position: relative;
            z-index: 1;
        }
        
        /* 响应式设计 */
        @media (max-width: 768px) {
            .hero-title {
                font-size: 2.5rem;
            }
            
            .nav-container {
                flex-direction: column;
                gap: 1rem;
            }
            
            .nav-links {
                flex-wrap: wrap;
                justify-content: center;
            }
            
            .anime-grid {
                grid-template-columns: 1fr;
                padding: 1rem;
            }
        }
    </style>
</head>
<body>
    <!-- 樱花飘落效果 -->
    <div id="sakura-container"></div>
    
    <!-- 导航栏 -->
    <nav class="anime-nav">
        <div class="nav-container">
            <div class="logo">
                <i class="fas fa-cherry-blossom"></i>樱色幻想
            </div>
            <div class="nav-links">
                <a href="#"><i class="fas fa-home"></i> 首页</a>
                <a href="#"><i class="fas fa-film"></i> 动漫推荐</a>
                <a href="#"><i class="fas fa-gamepad"></i> 游戏中心</a>
                <a href="#"><i class="fas fa-users"></i> 社区</a>
                <a href="#"><i class="fas fa-envelope"></i> 联系我们</a>
            </div>
        </div>
    </nav>
    
    <!-- 英雄区域 -->
    <section class="hero">
        <div class="hero-bg"></div>
        <h1 class="hero-title">欢迎来到动漫幻想世界</h1>
        <p class="hero-subtitle">这里有你最喜欢的动漫、游戏和二次元文化</p>
        <button class="anime-btn" onclick="showMessage()">
            <i class="fas fa-magic"></i> 开启奇幻之旅
        </button>
    </section>
    
    <!-- 内容卡片 -->
    <section class="anime-grid">
        <div class="anime-card">
            <div class="card-header">
                <i class="fas fa-star card-icon"></i>
            </div>
            <div class="card-body">
                <h3 class="card-title">新番推荐</h3>
                <p class="card-text">最新热门动漫速递，每周更新！包含剧情介绍、角色分析和观看链接。</p>
                <button class="anime-btn" style="margin-top: 1rem; padding: 8px 20px; font-size: 0.9rem;">
                    查看详情
                </button>
            </div>
        </div>
        
        <div class="anime-card">
            <div class="card-header">
                <i class="fas fa-gamepad card-icon"></i>
            </div>
            <div class="card-body">
                <h3 class="card-title">游戏中心</h3>
                <p class="card-text">二次元手游、主机游戏推荐，攻略教程、福利兑换码一网打尽！</p>
                <button class="anime-btn" style="margin-top: 1rem; padding: 8px 20px; font-size: 0.9rem;">
                    进入游戏
                </button>
            </div>
        </div>
        
        <div class="anime-card">
            <div class="card-header">
                <i class="fas fa-paint-brush card-icon"></i>
            </div>
            <div class="card-body">
                <h3 class="card-title">绘画教程</h3>
                <p class="card-text">从零开始学习动漫绘画，包含线稿、上色、人物设计等全套教程。</p>
                <button class="anime-btn" style="margin-top: 1rem; padding: 8px 20px; font-size: 0.9rem;">
                    开始学习
                </button>
            </div>
        </div>
        
        <div class="anime-card">
            <div class="card-header">
                <i class="fas fa-store card-icon"></i>
            </div>
            <div class="card-body">
                <h3 class="card-title">周边商城</h3>
                <p class="card-text">正版动漫周边、手办、画集、cosplay服装，限时折扣优惠！</p>
                <button class="anime-btn" style="margin-top: 1rem; padding: 8px 20px; font-size: 0.9rem;">
                    逛逛商店
                </button>
            </div>
        </div>
    </section>
    
    <!-- 页脚 -->
    <footer class="anime-footer">
        <div class="footer-bg"></div>
        <div class="footer-content">
            <h2 style="color: white; margin-bottom: 1rem;">加入我们的二次元大家庭</h2>
            <p style="color: rgba(255,255,255,0.9); margin-bottom: 2rem;">订阅获取最新动漫资讯和独家福利</p>
            
            <div style="display: flex; gap: 1rem; justify-content: center; margin-bottom: 2rem;">
                <button class="anime-btn" style="background: rgba(255,255,255,0.2); backdrop-filter: blur(10px);">
                    <i class="fab fa-discord"></i> Discord
                </button>
                <button class="anime-btn" style="background: rgba(255,255,255,0.2); backdrop-filter: blur(10px);">
                    <i class="fab fa-twitter"></i> Twitter
                </button>
                <button class="anime-btn" style="background: rgba(255,255,255,0.2); backdrop-filter: blur(10px);">
                    <i class="fab fa-bilibili"></i> Bilibili
                </button>
            </div>
            
            <p style="color: rgba(255,255,255,0.7); font-size: 0.9rem;">
                © 2023 樱色幻想动漫社区 | 一切内容仅供娱乐交流使用
            </p>
        </div>
    </footer>
    
    <script>
        // 樱花飘落效果
        function createSakura() {
            const sakura = document.createElement('div');
            sakura.classList.add('sakura');
            sakura.innerHTML = '🌸';
            
            // 随机大小
            const size = Math.random() * 20 + 10;
            sakura.style.fontSize = `${size}px`;
            
            // 随机位置
            sakura.style.left = `${Math.random() * 100}vw`;
            
            // 随机动画时长
            const duration = Math.random() * 10 + 10;
            sakura.style.animationDuration = `${duration}s`;
            
            // 随机透明度
            sakura.style.opacity = Math.random() * 0.5 + 0.3;
            
            document.getElementById('sakura-container').appendChild(sakura);
            
            // 移除樱花元素
            setTimeout(() => {
                sakura.remove();
            }, duration * 1000);
        }
        
        // 批量创建樱花
        setInterval(createSakura, 300);
        
        // 初始创建一些樱花
        for (let i = 0; i < 20; i++) {
            setTimeout(createSakura, i * 100);
        }
        
        // 按钮点击效果
        function showMessage() {
            const messages = [
                '🎉 欢迎来到二次元世界！',
                '🌸 樱花飘落，梦想起航！',
                '⭐ 发现新的动漫宝藏！',
                '🎨 一起创造美好回忆！'
            ];
            const message = messages[Math.floor(Math.random() * messages.length)];
            
            // 创建浮动消息
            const floatMsg = document.createElement('div');
            floatMsg.textContent = message;
            floatMsg.style.cssText = `
                position: fixed;
                top: 50%;
                left: 50%;
                transform: translate(-50%, -50%);
                background: rgba(255, 183, 197, 0.95);
                color: white;
                padding: 20px 40px;
                border-radius: 50px;
                font-size: 1.5rem;
                z-index: 10000;
                animation: floatUp 1.5s ease forwards;
                box-shadow: 0 10px 30px rgba(255, 183, 197, 0.5);
            `;
            
            // 添加动画
            const style = document.createElement('style');
            style.textContent = `
                @keyframes floatUp {
                    0% { opacity: 0; transform: translate(-50%, -40%); }
                    20% { opacity: 1; transform: translate(-50%, -50%); }
                    80% { opacity: 1; transform: translate(-50%, -50%); }
                    100% { opacity: 0; transform: translate(-50%, -60%); }
                }
            `;
            document.head.appendChild(style);
            
            document.body.appendChild(floatMsg);
            
            // 移除消息
            setTimeout(() => {
                floatMsg.remove();
                style.remove();
            }, 1500);
        }
        
        // 卡片悬停音效（可选）
        const cards = document.querySelectorAll('.anime-card');
        cards.forEach(card => {
            card.addEventListener('mouseenter', () => {
                // 可以添加音效，这里用console模拟
                console.log('✨ 卡片悬停效果');
            });
        });
        
        // 滚动动画
        window.addEventListener('scroll', () => {
            const scrolled = window.pageYOffset;
            const rate = scrolled * -0.5;
            document.querySelector('.hero-bg').style.transform = `translateY(${rate}px)`;
        });
    </script>
</body>
</html>
